import os
os.system('cls')

numerox = int(input('Escolha qualquer número inteiro: '))
porcentagem = int(input('Escolha qual % quer: '))

porcento = numerox * (porcentagem/100)

print(f'Essa é a porcentagem de {numerox}: {porcento:.2f}')